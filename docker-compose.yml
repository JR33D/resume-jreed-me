version: "3"
services:
    nginx:
        depends_on:
          - api
          - client
        restart: always
        build:
          dockerfile: dockerfile
          context: ./nginx
        ports:
          - "3050:80"

    client:
        container_name: resume-jreed-me_client
        build:
            context: ./client
            dockerfile: dockerfile
        #ports:
        #    - "3000:3000"
        volumes:
            - ./client:/app

    api:
        container_name: resume-jreed-me-api
        build:
            context: ./server
            dockerfile: dockerfile
        #ports:
        #    - "3001:3001"
        volumes:
            - ./server:/app